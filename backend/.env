# backend/.env
FLASK_ENV=development
JWT_SECRET_KEY=replace_this_with_a_random_long_secret
# Optionally: set a custom DB file or use an external DB
# DATABASE_URI=sqlite:////absolute/path/to/database.db
